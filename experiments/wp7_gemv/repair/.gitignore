# Campaign trap 7: this campaign has TWICE shipped a bloated PR diff from
# tracked build artefacts. Nothing this directory produces that is a binary, a
# profiler report or a stderr spool may become tracked. The repair pass builds
# no binary of its own -- it reuses ../ab/gemvab_v, which ../ab/.gitignore
# already covers.
*.ncu-rep
*.nsys-rep
*.sqlite
*.qdrep
parity_err.txt
gemvab_*
