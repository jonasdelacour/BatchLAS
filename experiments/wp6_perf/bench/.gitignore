# Compiled harnesses and profiler blobs. Same rule as experiments/wp6_lu/bench/,
# experiments/wp5_qr/bench/ and experiments/wp4_potrf/slm/: the README, the cell
# lists, the CSVs and the derived summaries are committed; binaries and blobs
# never are. This branch has been caught twice, once with 13 trace files that
# were 83 % of the PR diff.
lubench6_v
lubench6_nv
blindguard
*.nsys-rep
*.sqlite
*.qdstrm
*.qdrep
nsys/
*.json
