#pragma once

// This file is auto-generated by CMake - do not edit manually.
// It contains configure-time device limit data derived from sycl-ls.

#include <array>
#include <cstddef>
#include <cstdint>

namespace batchlas::device_limits {

struct DetectedDeviceLimit {
    const char* platform_name;
    const char* device_type;
    const char* device_name;
    const char* architecture;
    std::uint32_t subgroup_size;
    std::uint64_t local_mem_bytes;
    std::uint64_t subgroup_workspace_budget_bytes;
};

inline constexpr std::array<DetectedDeviceLimit, @BATCHLAS_DEVICE_LIMIT_ENTRY_COUNT@> DETECTED_DEVICES = {{
@BATCHLAS_DEVICE_LIMIT_ENTRY_LINES@}};

inline constexpr std::size_t DETECTED_DEVICE_COUNT = DETECTED_DEVICES.size();
inline constexpr std::uint64_t MIN_GPU_LOCAL_MEM_BYTES = @BATCHLAS_DETECTED_DEVICE_LIMIT_MIN_GPU_LOCAL_MEM_BYTES@ull;
inline constexpr std::uint64_t MIN_GPU_SUBGROUP_WORKSPACE_BUDGET_BYTES =
    @BATCHLAS_DETECTED_DEVICE_LIMIT_MIN_GPU_SUBGROUP_WORKSPACE_BUDGET_BYTES@ull;
inline constexpr std::uint32_t MIN_GPU_SAFE_SUBGROUPS_PER_WORKGROUP =
    @BATCHLAS_DETECTED_DEVICE_LIMIT_MIN_GPU_SAFE_SUBGROUPS_PER_WORKGROUP@u;

inline constexpr std::uint64_t subgroup_workspace_budget_bytes() {
    return MIN_GPU_SUBGROUP_WORKSPACE_BUDGET_BYTES;
}

inline constexpr std::uint32_t safe_subgroups_per_workgroup() {
    return MIN_GPU_SAFE_SUBGROUPS_PER_WORKGROUP;
}

} // namespace batchlas::device_limits